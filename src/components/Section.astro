---
type Props = {
  title?: string
}
const { title } = Astro.props
---

<section class="mx-auto w-full max-w-2xl">
  { title && <h2 class="text-2xl font-sans font-bold mb-2 print:mb-1 print:text-xl">{ title }</h2> }
  <slot />
</section>
